(function(){'use strict';const form=document.getElementById('customer-login-form'),email=document.getElementById('customer-login-email'),message=document.getElementById('customer-login-message');function show(text,type=''){message.hidden=!text;message.textContent=text;message.className=`customer-message${type?` ${type}`:''}`;}const error=new URLSearchParams(location.search).get('error');if(error)show(error,'error');form.addEventListener('submit',async event=>{event.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button'),label=button.textContent;button.disabled=true;button.textContent='Sending…';show('');try{const response=await fetch('/api/customer/auth/request',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({email:email.value.trim()})}),data=await response.json().catch(()=>({}));show(data.message||'If that email is connected to a WestTech order, a secure sign-in link is on the way.');form.reset();}catch{show('The sign-in request could not be sent. Please try again.','error');}finally{button.disabled=false;button.textContent=label;}});})();
